public void plusOne(List<Integer> numbers) {
    // code here
    for(Integer i = numbers.size(); i > = 0; i-- ){
        numbers[i]++;
        if(numbers[i] < = 9)
            return;
        numbers[i] = 0;
    } 
    numbers.add(0, 1);   
}