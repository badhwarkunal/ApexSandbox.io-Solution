public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    boolean re = (con.AccountId == null? false : c.AccountId == opp.AccountId);
    if(re){
        return acc.Id = con.AccountId;
    }
    else 
        return false;
}