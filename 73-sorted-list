public boolean isSorted(List<Integer> numbers) {
    //code here
    List<Integer> sortNumbers = numbers.clone();
    sortNumbers.sort();
    if(numbers.size()<2)
        return true;
    else
        return sortNumbers[0] == numbers[0] || sortNumbers[0] == numbers[numbers.size()-1];
}