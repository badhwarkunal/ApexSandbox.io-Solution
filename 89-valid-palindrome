public Boolean isPalindrome(String str){
//code here
    for(Integer i =0 ; i< (str.length()/2);i++){
        if(str[i] != str[str.length()-i]){
            return false; 
        }
    }
    return true;
}